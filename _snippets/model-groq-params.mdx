| 参数               | 类型                              | 默认值                                   | 描述                                                                                                                                                                                  |
| ------------------- | --------------------------------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`                | `str`                             | `"llama-3.3-70b-versatile"` | 用于生成响应的特定模型ID。                                                                                                                                         |
| `name`              | `str`                             | `"Groq"`                                  | 智能体的名称标识符。                                                                                                                                                           |
| `provider`          | `str`                             | `"Groq"`                                  | 模型的提供商。                                                                                                                                                                   |
| `frequency_penalty` | `Optional[float]`                 | `None`                                    | 介于-2.0到2.0之间的数值。正值会根据当前文本中已出现的词频惩罚新token，降低模型逐字重复同一句话的可能性。 |
| `logit_bias`        | `Optional[Any]`                   | `None`                                    | JSON对象，通过将token ID映射到-100至100之间的偏置值，来修改指定token出现在补全结果中的概率。                                         |
| `logprobs`          | `Optional[bool]`                  | `None`                                    | 是否返回输出token的log概率值。                                                                                                                                    |
| `max_tokens`        | `Optional[int]`                   | `None`                                    | 聊天补全中生成的最大token数量。                                                                                                                             |
| `presence_penalty`  | `Optional[float]`                 | `None`                                    | 介于-2.0到2.0之间的数值。正值会根据当前文本中是否出现该token进行惩罚，增加模型讨论新话题的可能性。              |
| `response_format`   | `Optional[Dict[str, Any]]`        | `None`                                    | 指定模型必须输出的格式。设为`{ "type": "json_object" }`可启用JSON模式，确保生成的消息是有效JSON。                                     |
| `seed`              | `Optional[int]`                   | `None`                                    | 确定性采样的种子值，确保相同种子和参数的重复请求返回相同结果。                                                                |
| `stop`              | `Optional[Union[str, List[str]]]` | `None`                                    | 最多4个序列，当API生成到这些序列时会停止生成后续token。                                                                                                                         |
| `temperature`       | `Optional[float]`                 | `None`                                    | 采样温度值，介于0到2之间。较高值(如0.8)使输出更随机，较低值(如0.2)使输出更集中和确定。                    |
| `top_logprobs`      | `Optional[int]`                   | `None`                                    | 为每个生成的token返回的最高log概率值数量。                                                                                                                      |
| `top_p`             | `Optional[float]`                 | `None`                                    | 核采样参数。模型仅考虑概率质量达到top_p的token结果。                                                                                       |
| `user`              | `Optional[str]`                   | `None`                                    | 终端用户的唯一标识符，用于监控和检测滥用行为。                                                                                                         |
| `extra_headers`     | `Optional[Any]`                   | `None`                                    | API请求中包含的附加头信息。                                                                                                                                               |
| `extra_query`       | `Optional[Any]`                   | `None`                                    | API请求中包含的附加查询参数。                                                                                                                                      |
| `request_params`    | `Optional[Dict[str, Any]]`        | `None`                                    | 请求中包含的附加参数。                                                                                                                                             |
| `api_key`           | `Optional[str]`                   | `None`                                    | 用于服务请求认证的API密钥。                                                                                                                                      |
| `base_url`          | `Optional[Union[str, httpx.URL]]` | `None`                                    | 向服务发起API请求的基础URL。                                                                                                                                         |
| `timeout`           | `Optional[int]`                   | `None`                                    | 请求超时时长，单位为秒。                                                                                                                                     |
| `max_retries`       | `Optional[int]`                   | `None`                                    | 失败请求的最大重试次数。                                                                                                                                    |
| `default_headers`   | `Optional[Any]`                   | `None`                                    | 所有API请求中包含的默认头信息。                                                                                                                                              |
| `default_query`     | `Optional[Any]`                   | `None`                                    | 所有API请求中包含的默认查询参数。                                                                                                                                     |
| `http_client`       | `Optional[httpx.Client]`          | `None`                                    | 用于发起API请求的自定义HTTP客户端。                                                                                                                                                |
| `client_params`     | `Optional[Dict[str, Any]]`        | `None`                                    | 客户端配置的附加参数。                                                                                                                                              |
| `client`            | `Optional[GroqClient]`            | `None`                                    | 用于发起API请求的GroqClient实例。                                                                                                                                  |
| `async_client`      | `Optional[AsyncGroqClient]`       | `None`                                    | 用于发起异步API请求的AsyncGroqClient实例。                                                                                                                |