---
title: 什么是智能体？
sidebarTitle: 概述
description: 了解 Agno 智能体及其工作原理。
---
**智能体**是能够自主运行的人工智能程序。传统软件遵循预先编程的步骤序列，而智能体则通过机器学习**模型**动态决定其行动路径。

智能体的核心由**模型**、**工具**和**指令**构成：

- **模型**：控制执行流程。它决定何时进行推理、采取行动或作出响应。
- **工具**：使智能体能够执行操作并与外部系统交互。
- **指令**：是我们编程智能体的方式，教导其如何使用工具和作出响应。

智能体还具备**记忆**、**知识**、**存储**和**推理**能力：

- **推理**：使智能体在响应前能"思考"，并"分析"其行动结果（如工具调用），从而提高响应的可靠性和质量。
- **知识**：是特定领域的信息，智能体可在**运行时进行搜索**以做出更优决策并提供准确响应（RAG）。知识存储在向量数据库中，这种**运行时搜索**模式被称为Agentic RAG/Agentic Search。
- **存储**：用于将会话历史记录和状态保存到数据库中。模型API是无状态的，存储使我们能从中断处继续对话。这让智能体具备状态性，支持多轮长期对话。
- **记忆**：赋予智能体存储和调用先前交互信息的能力，使其能学习用户偏好并个性化响应。

<img height="200" src="/images/agent.png" style={{ borderRadius: "8px" }} />

<Check>
如果是首次构建智能体，[请先参考这些示例](/introduction/agents#basic-agent)再学习进阶概念。
</Check>

## 示例：研究型智能体

我们将使用Exa构建一个研究型智能体，演示如何引导智能体生成特定格式的报告。在高级场景中，应改用[结构化输出](/agents/structured-output)。

<Note>
  描述和指令会被转换为系统消息，输入则作为用户消息传递。设置 `debug_mode=True` 可查看后台日志。
</Note>

<Steps>
  <Step title="创建研究型智能体">
    创建文件 `research_agent.py`

    ```python research_agent.py
    from datetime import datetime
    from pathlib import Path
    from textwrap import dedent

    from agno.agent import Agent
    from agno.models.openai import OpenAIChat
    from agno.tools.exa import ExaTools

    today = datetime.now().strftime("%Y-%m-%d")

    agent = Agent(
        model=OpenAIChat(id="gpt-4o"),
        tools=[ExaTools(start_published_date=today, type="keyword")],
        description=dedent("""\
            You are Professor X-1000, a distinguished AI research scientist with expertise
            in analyzing and synthesizing complex information. Your specialty lies in creating
            compelling, fact-based reports that combine academic rigor with engaging narrative.

            Your writing style is:
            - Clear and authoritative
            - Engaging but professional
            - Fact-focused with proper citations
            - Accessible to educated non-specialists\
        """),
        instructions=dedent("""\
            Begin by running 3 distinct searches to gather comprehensive information.
            Analyze and cross-reference sources for accuracy and relevance.
            Structure your report following academic standards but maintain readability.
            Include only verifiable facts with proper citations.
            Create an engaging narrative that guides the reader through complex topics.
            End with actionable takeaways and future implications.\
        """),
        expected_output=dedent("""\
        A professional research report in markdown format:

        # {引人注目且精准概括主题的标题}

        # # 执行摘要
        {Brief overview of key findings and significance}

        # # 简介
        {Context and importance of the topic}
        {Current state of research/discussion}

        # # 关键发现
        {Major discoveries or developments}
        {Supporting evidence and analysis}

        # # 影响
        {Impact on field/society}
        {Future directions}

        # # 核心要点
        - {Bullet point 1}
        - {Bullet point 2}
        - {Bullet point 3}

        # # 参考文献
        - [Source 1](link) - Key finding/quote
        - [Source 2](link) - Key finding/quote
        - [Source 3](link) - Key finding/quote

        ---
        Report generated by Professor X-1000
        Advanced Research Systems Division
        Date: {current_date}\
        """),
        markdown=True,
        show_tool_calls=True,
        add_datetime_to_instructions=True,
    )

    # 示例用法
    if __name__ == "__main__":
        # 生成一份前沿技术主题的研究报告
        agent.print_response(
            "Research the latest developments in brain-computer interfaces", stream=True
        )

    # 更多可尝试的示例提示：
    """
    Try these research topics:
    1. "Analyze the current state of solid-state batteries"
    2. "Research recent breakthroughs in CRISPR gene editing"
    3. "Investigate the development of autonomous vehicles"
    4. "Explore advances in quantum machine learning"
    5. "Study the impact of artificial intelligence on healthcare"
    """
    ```

  </Step>
  <Step title="运行智能体">
    安装库

    ```shell
    pip install openai exa-py agno
    ```

    运行智能体

    ```shell
    python research_agent.py
    ```

  </Step>
</Steps>