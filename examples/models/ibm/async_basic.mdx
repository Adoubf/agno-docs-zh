---
title: 异步基础智能体
---
## 代码

```python cookbook/models/ibm/watsonx/async_basic.py
import asyncio

from agno.agent import Agent, RunResponse
from agno.models.ibm import WatsonX

agent = Agent(model=WatsonX(id="ibm/granite-20b-code-instruct"), markdown=True)

# 将响应存储在变量中
# 运行: RunResponse = 智能体.run("分享一个两句恐怖故事")
# 打印(run.content)

# 在终端中打印响应
asyncio.run(agent.aprint_response("Share a 2 sentence horror story"))
```

## 使用说明

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="设置您的 API 密钥">
    ```bash
    export IBM_WATSONX_API_KEY=xxx
    export IBM_WATSONX_PROJECT_ID=xxx
    ```
  </Step>

  <Step title="安装库">
    ```bash
    pip install -U ibm-watsonx-ai agno
    ```
  </Step>

  <Step title="运行智能体">
    <CodeGroup>
    ```bash 苹果电脑
    python cookbook/models/ibm/watsonx/async_basic.py
    ```

    ```bash Windows
    python cookbook\models\ibm\watsonx\async_basic.py
    ```
    </CodeGroup>
  </Step>
</Steps>

本示例展示如何结合 IBM WatsonX 使用 Agno 的异步 API。它会创建一个智能体，并通过 `asyncio.run()` 来执行异步的 `aprint_response` 方法。