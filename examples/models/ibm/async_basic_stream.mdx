---
title: 异步流式智能体
---
## 代码

```python cookbook/models/ibm/watsonx/async_basic_stream.py
import asyncio

from agno.agent import Agent, RunResponse
from agno.models.ibm import WatsonX

agent = Agent(
    model=WatsonX(id="ibm/granite-20b-code-instruct"), debug_mode=True, markdown=True
)

# 将响应存储在变量中
# run_response: Iterator[RunResponse] = agent.run("分享一个两句话的恐怖故事", stream=True)
# for chunk in run_response:
# print(chunk.content)

# 在终端打印响应
asyncio.run(agent.aprint_response("Share a 2 sentence horror story", stream=True))
```

## 使用方法

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="设置您的 API 密钥">
    ```bash
    export IBM_WATSONX_API_KEY=xxx
    export IBM_WATSONX_PROJECT_ID=xxx
    ```
  </Step>

  <Step title="安装库">
    ```bash
    pip install -U ibm-watsonx-ai agno
    ```
  </Step>

  <Step title="运行智能体">
    <CodeGroup>
    ```bash 苹果电脑
    python cookbook/models/ibm/watsonx/async_basic_stream.py
    ```

    ```bash Windows
    python cookbook\models\ibm\watsonx\async_basic_stream.py
    ```
    </CodeGroup>
  </Step>
</Steps>

这个示例结合了异步执行与流式传输。它创建了一个带有`debug_mode=True`的智能体用于附加日志记录，并使用异步API的流式传输功能来实时获取和显示生成的响应。