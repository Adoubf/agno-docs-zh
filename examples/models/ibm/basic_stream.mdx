---
title: 基础智能体流式处理
---
## 代码

```python cookbook/models/ibm/watsonx/basic_stream.py
from typing import Iterator
from agno.agent import Agent, RunResponse
from agno.models.ibm import WatsonX

agent = Agent(model=WatsonX(id="ibm/granite-20b-code-instruct"), markdown=True)

# 将响应存储到变量中
# run_response: Iterator[RunResponse] = agent.run("分享一个两句话的恐怖故事", stream=True)
# for chunk in run_response:
# 打印(chunk.content)

# 在终端中打印响应
agent.print_response("Share a 2 sentence horror story", stream=True)
```

## 使用说明

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="设置您的API密钥">
    ```bash
    export IBM_WATSONX_API_KEY=xxx
    export IBM_WATSONX_PROJECT_ID=xxx
    ```
  </Step>

  <Step title="安装库">
    ```bash
    pip install -U ibm-watsonx-ai agno
    ```
  </Step>

  <Step title="运行智能体">
    <CodeGroup>
    ```bash 苹果电脑
    python cookbook/models/ibm/watsonx/basic_stream.py
    ```

    ```bash Windows
    python cookbook\models\ibm\watsonx\basic_stream.py
    ```
    </CodeGroup>
  </Step>
</Steps>

本示例展示了如何配合 IBM WatsonX 使用流式传输功能。在调用 `print_response()` 或 `run()` 时设置 `stream=True` 可启用逐令牌流式传输，从而提供更具交互性的用户体验。