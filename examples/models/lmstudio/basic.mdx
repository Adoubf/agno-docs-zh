---
title: 基础智能体
---

## 代码

```python
from agno.agent import Agent, RunResponse  # noqa
from agno.models.lmstudio import LMStudio

agent = Agent(model=LMStudio(id="qwen2.5-7b-instruct-1m"), markdown=True)

# 将响应存储到变量中
# 运行：RunResponse = agent.run("分享一个两句话的恐怖故事")
# 打印(run.content)

# 在终端打印响应
agent.print_response("Share a 2 sentence horror story")
```

## 使用说明

<Steps>
  <Snippet file="create-venv-step.mdx" />

  <Step title="安装 LM Studio">
    从 <a href="https://lmstudio.ai/download">此链接</a> 安装 LM Studio 并下载您要使用的模型。
  </Step>

<Step title="安装库">```bash pip install -U agno ```</Step>

  <Step title="运行智能体">
    <CodeGroup>
    ```bash Mac
    python cookbook/models/lmstudio/basic.py
    ```

    ```bash Windows
    python cookbook/models/lmstudio/basic.py
    ```
    </CodeGroup>

  </Step>
</Steps>
