---
title: Zendesk
---
**Zendesk工具**使智能体能够访问Zendesk API以搜索文章。

## 前提条件

以下示例需要`requests`库和认证凭证。

```shell
pip install -U requests
```

```shell
export ZENDESK_USERNAME=***
export ZENDESK_PW=***
export ZENDESK_COMPANY_NAME=***
```

## 示例

以下智能体将在Zendesk中搜索"如何登录？"并打印响应。

```python cookbook/tools/zendesk_tools.py
from agno.agent import Agent
from agno.tools.zendesk import ZendeskTools

agent = Agent(tools=[ZendeskTools()], show_tool_calls=True)
agent.print_response("How do I login?", markdown=True)
```

## 工具包参数

| 参数      | 类型  | 默认值 | 描述                                                             |
| -------------- | ----- | ------- | ----------------------------------------------------------------------- |
| `username`     | `str` | -       | 用于认证或识别目的的用户名。        |
| `password`     | `str` | -       | 与用户名关联的密码，用于认证目的。  |
| `company_name` | `str` | -       | 与用户或所访问数据相关的公司名称。 |

## 工具包功能

| 功能         | 描述                                                                                    |
| ---------------- | ---------------------------------------------------------------------------------------------- |
| `search_zendesk` | 该函数在Zendesk帮助中心搜索与给定字符串匹配的文章。 |

## 开发者资源

- 查看[工具](https://github.com/agno-agi/agno/blob/main/libs/agno/agno/tools/zendesk.py)
- 查看[手册](https://github.com/agno-agi/agno/blob/main/cookbook/tools/zendesk_tools.py)