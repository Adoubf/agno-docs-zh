---
title: Composio
---
[**ComposioTools**](https://docs.composio.dev/framework/phidata) 使智能体能够使用 Gmail、Salesforce、Github 等工具。

## 先决条件

以下示例需要 `composio-agno` 库。

```shell
pip install composio-agno
composio add github # Login into Github
```

## 示例

以下智能体将使用 Composio 工具包中的 Github 工具为一个仓库加星标。

```python cookbook/tools/composio_tools.py
from agno.agent import Agent
from composio_agno import Action, ComposioToolSet


toolset = ComposioToolSet()
composio_tools = toolset.get_tools(
  actions=[Action.GITHUB_STAR_A_REPOSITORY_FOR_THE_AUTHENTICATED_USER]
)

agent = Agent(tools=composio_tools, show_tool_calls=True)
agent.print_response("Can you star agno-agi/agno repo?")
```

## 工具包参数

调用 GitHub 加星标仓库动作时使用以下参数：

| 参数 | 类型 | 默认值 | 说明 |
|-----------|------|---------|-------------|
| `owner` | `str` | - | 要加星标的仓库所有者。 |
| `repo` | `str` | - | 要加星标的仓库名称。 |

## 工具包功能

Composio 工具包提供 1000+ 种功能来连接不同的软件工具。
点击此[链接](https://composio.dev/tools)查看完整的功能列表。