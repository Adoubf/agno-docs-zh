---
title: OpenAI 响应
description: 了解如何在 Agno 中使用 OpenAI 响应。
---
`OpenAIResponses` 是一个通过 Responses API 与 OpenAI 模型交互的类。该类为使用 OpenAI 新版 Responses API（与传统 Chat API 不同）提供了简化的接口，支持工具调用、文件处理和知识检索等高级功能。

## 认证

设置您的 `OPENAI_API_KEY` 环境变量。您可以从 [OpenAI 这里](https://platform.openai.com/account/api-keys) 获取密钥。

<CodeGroup>

```bash Mac
export OPENAI_API_KEY=sk-***
```

```bash Windows
setx OPENAI_API_KEY sk-***
```

</CodeGroup>

## 示例

将 `OpenAIResponses` 与您的 `Agent` 配合使用：

<CodeGroup>

```python agent.py

from agno.agent import Agent
from agno.media import File
from agno.models.openai.responses import OpenAIResponses

agent = Agent(
    model=OpenAIResponses(id="gpt-4o-mini"),
    tools=[{"type": "file_search"}, {"type": "web_search_preview"}],
    markdown=True,
)

agent.print_response(
    "Summarize the contents of the attached file and search the web for more information.",
    files=[File(url="https://agno-public.s3.amazonaws.com/recipes/ThaiRecipes.pdf")],
)

```

</CodeGroup>

<Note> 查看更多示例 [请访问此处](../examples/models/openai/responses)。 </Note>

## 参数

更多信息请参阅 [OpenAI Responses 文档](https://platform.openai.com/docs/api-reference/responses)。

<Snippet file="model-openai-responses-params.mdx" />

`OpenAIResponses` 是 [Model](/reference/models/model) 类的子类，拥有相同的参数访问权限。